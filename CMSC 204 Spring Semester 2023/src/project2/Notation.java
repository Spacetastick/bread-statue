package project2;

//A utility class that converts an infix expression to a postfix expression, a postfix expression to 
//an infix expression and evaluates a postfix expression.
public class Notation {

	//confirm these signatures with provided javadocs
	
	//converts infix notation to postfix notation
	//InvalidNotationFormatException - if the infix expression format is invalid
	public static String convertInfixToPostfix(String infix) throws InvalidNotationFormatException {
		//must use a queue for the internal structure that holds the 
		//postfix solution.
		//then use tostring method of the queue to return the solution as a string
	}
	
	//Convert the Postfix expression to the Infix expression
	//InvalidNotationFormatException - if the postfix expression format is invalid
	public static String convertPostfixToInfix(String postfix) throws InvalidNotationFormatException {
		
	}
	
	//check docs
	//evaluates postfix expression from a string to a double
	//InvalidNotationFormatException - if the postfix expression format is invalid
	public static double evaluatePostfix(String postfixExpression) throws InvalidNotationFormatException {
		
	}
	
	@Override
	public String toString() {
		
	}
}
